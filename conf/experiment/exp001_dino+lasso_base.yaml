# @package _global_

# 1. 使うコンポーネントを強制的に指定 (override)
defaults:
  - override /feature: dino_giant
  - override /model: lasso
  - override /training: lasso.yaml # Lasso用の学習設定(n_splitsなど)
  - override /inference: null # 推論設定は使わない

# 3. 保存フォルダ名 (ファイル名と同じにするのが通例)
exp_name: "exp001_dino+lasso_base"
