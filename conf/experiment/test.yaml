# @package _global_

defaults:
  - override /dir: kaggle
  - override /dataset: deual
  - override /model: dino_base
  - override /training: test_run
  - override /transform: base_transform # ← ★ここが重要！

exp_name: "test"
wandb_mode: "online"
seed: 42
