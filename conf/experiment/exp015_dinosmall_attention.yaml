# @package _global_
defaults:
  - override /dir: kaggle
  - override /dataset: deual
  - override /model: dino_attention
  - override /training: default
  - override /transform: transform

exp_name: "exp015_dino_small_attention"
wandb_mode: "online"
seed: 42
